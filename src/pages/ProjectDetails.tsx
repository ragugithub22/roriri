import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

const ProjectDetails = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Project Details</CardTitle>
        <CardDescription>View and manage project information and progress.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Project details management coming soon...</p>
      </CardContent>
    </Card>
  );
};

export default ProjectDetails;
