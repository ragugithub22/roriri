import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

const Coordinator = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Coordinator</CardTitle>
        <CardDescription>Manage coordinators and their responsibilities.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Coordinator management coming soon...</p>
      </CardContent>
    </Card>
  );
};

export default Coordinator;
