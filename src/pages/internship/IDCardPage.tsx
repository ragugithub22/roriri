import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function IDCardPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>ID Card Details</CardTitle>
        <CardDescription>Manage intern ID card information</CardDescription>
      </CardHeader>
      <CardContent>
        <p>ID card management coming soon...</p>
      </CardContent>
    </Card>
  );
}
